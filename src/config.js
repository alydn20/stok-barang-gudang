// Ganti dengan URL Google Apps Script Web App Anda
// Cara deploy: buka Google Sheet > Extensions > Apps Script > Deploy > New deployment > Web app
export const GAS_URL = import.meta.env.VITE_GAS_URL || ''
